# `@graviola/calc-fixtures`

Private golden fixtures for calc-package and datastore contract tests.

Use as a **devDependency** in library packages that run calc unit tests.
Demo apps (e.g. `testapp`) may depend on it for sample schemas — it is not a
publishable domain model.
