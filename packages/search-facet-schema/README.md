# @graviola/search-facet-schema

TypeScript types and runtime (AJV) loader for `SearchFacetSchema` documents used to configure
facet scopes and full-text indexing metadata alongside an entity JSON Schema.
