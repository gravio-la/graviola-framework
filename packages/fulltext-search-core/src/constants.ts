/** Meilisearch / index document carrier for entity IRI (JSON-LD @id source). */
export const INDEX_DOC_IRI = "__iri";

/** Meilisearch / index document carrier for type IRI (JSON-LD @type source). */
export const INDEX_DOC_TYPE = "__type";
