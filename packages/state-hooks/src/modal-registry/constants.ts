/** NiceModal.register id for entity detail (read-only modal shell). */
export const MODAL_ENTITY_DETAIL = "graviola:entity-detail";

/** NiceModal.register id for entity edit dialog. */
export const MODAL_EDIT_ENTITY = "graviola:edit-entity";

/** NiceModal id for the similarity / entity-finder side panel (single instance). */
export const MODAL_SIMILARITY_FINDER = "graviola:similarity-finder";
