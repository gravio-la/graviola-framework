# @graviola/store-core

Capability-composed `Store` types (`BaseStore`, `SparqlStore<R>` for the full SPARQL intersection), `ReadResult<T>`, runtime capability descriptors, cheap capability simulators, and entity change events.

Layer 1 — no React; safe for Bun and browser.
