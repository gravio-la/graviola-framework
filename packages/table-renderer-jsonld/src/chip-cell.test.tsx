import { describe, expect, test } from "bun:test";
import { JsonLdChipCell } from "./index";

describe("JsonLdChipCell", () => {
  test("is defined", () => {
    expect(JsonLdChipCell).toBeDefined();
  });
});
